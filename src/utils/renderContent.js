/**
 * Parses article content and converts [[id|text]] syntax to clickable links
 * Implementation in T-011
 */
// eslint-disable-next-line no-unused-vars
export function renderContent(text, onArticleClick) {
  return text
}
